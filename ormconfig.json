{
  "type": "sqlite",
  "host": "localhost",
  "database": "src/database/database.sqlite",
  "migrations": [
    "src/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "src/database/migrations"
  }
}